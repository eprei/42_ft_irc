#ifndef CLIENT_HPP
# define CLIENT_HPP

# include "Includes.hpp"

class Client
{
	public:

		Client();
		Client(Client &other);
		Client &operator=(Client &other);
		~Client();

	private:

};

#endif
