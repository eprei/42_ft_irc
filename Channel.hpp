#ifndef CHANNEL_HPP
# define CHANNEL_HPP

# include "Includes.hpp"

class Channel
{
	public:

		Channel();
		Channel(Channel &other);
		Channel &operator=(Channel &other);
		~Channel();

	private:

};

#endif
